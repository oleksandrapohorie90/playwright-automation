Common methods of locator obj:

    Actions:
    click() - 
    fill()
    type()
    press()
    check() - handling checkBoxes, verify that checkbox is not checked bf it clicks it, will only click if not checked
    uncheck()
    selectOption() - used for dropdowns that has select tagName, equivalent to selenium Select class
Select by class by value, index, visibleText - same for playwright select by value, text, index

    Retrieval:
    textContent()
    innerText()
    inputValue()
    getAttribute()

    State:
    isVisible()
    isEnabled()
    isChecked()
    isDisabled()
