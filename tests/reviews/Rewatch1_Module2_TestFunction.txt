The test() fucntion is the core building block of playwright tests;
It takes two arguments: a string description and an async callback fucntion 
        description: describes what the test is checkingl
        async callback function: takes fixtures and contains the codes/scripts of the test
Each test function runs in isolation, Playwright creates a new browser context for each test

import {test} from '@playwright/test';
test('test name', async ({ fixtureName }) => {

    //Test Codes
});

//fixture -> page is equivalent to driver variable in Selenium, will be able to control a tab in the web browser; 
mouse actions etc there are methods available in this fixture; its automatically created and destroyed; no need to tearDown steps;
lots of assertions, validations
test('My Automation test', async ({ page }) => {

    //Test Codes
});
Remember most of the methods return promise, so we need to await them, give sometime for promise to be resolved;
If not we can just return them - also thats why we have sync
test("Simple google test", async ({page}) => {
    //navigate to the specific url, method returns promise, so we need to await it
    await
    page.goto("https://www.google.com/");
    //if return time returns promise we need to await it, if not we can just return it
await page.waitForTimeout(3000);
});

=====================
import {test} from "@playwright/test"; //new way to import test from playwright, 
it is the same as the old way, 
but it is more modern and it is recommended to use it in new projects, it is also more compatible with modern JavaScript features like ES modules and async/await, so we can use it without any issues in our tests.

import
{test} - what I would like to import
from "@aplywright/test" - where I would like to import that member from ? which library

=====================
NamingConvensions
For folder names: lowercase
For test files: lowerCamelCase or Kebab-case
        lowerCamelCase: loginTest.spec.js, loginTest.test.js
        Kebab-case: login-test.spec.js, login-test.test.js
For page object files: UpperCamelCase
        UpperCamelCase: LoginPage.js